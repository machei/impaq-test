'use strict';

describe('app module', function() {
  beforeEach(module('app'));
  describe('useredit controller', function(){
    it('should ....', inject(function($controller) {
      var ctrl = $controller('UserEditCtrl');
      expect(ctrl).toBeDefined();
    }));
  });
});
